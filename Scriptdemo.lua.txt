loadstring(game:HttpGet(("https://raw.githubusercontent.com/daucobonhi/Ui-Redz-V2/refs/heads/main/UiREDzV2.lua")))()

       local Window = MakeWindow({
         Hub = {
         Title = "Vân tổng hợp",
         Animation = "Cat bắc vân"
         },
        Key = {
        KeySystem = false,
        Title = "Key System",
        Description = "",
        KeyLink = "",
        Keys = {"1234"},
        Notifi = {
        Notifications = true,
        CorrectKey = "Running the Script...",
       Incorrectkey = "The key is incorrect",
       CopyKeyLink = "Copied to Clipboard"
      }
    }
  })

       MinimizeButton({
       Image = "http://www.roblox.com/asset/?id=1000010861",
       Size = {60, 60},
       Color = Color3.fromRGB(10, 10, 10),
       Corner = true,
       Stroke = false,
       StrokeColor = Color3.fromRGB(255, 0, 0)
      })
      
------ Tab
     local Tab1o = MakeTab({Name = "Script Farm"})
     local Tab2o = MakeTab({Name = "Script kaitun"})
     local Tab3o = MakeTab({Name = "Fix lag"})   
------- BUTTON
    
    AddButton(Tab1o, {
     Name = "W-Azure Hub",
    Callback = function()
	  local Settings = {
  JoinTeam = "Pirates"; -- Pirates/Marines
  Translator = true; -- true/false
}

loadstring(game:HttpGet("https://raw.githubusercontent.com/LuaAnarchist/YeuEmNhieuLam/refs/heads/main/w-azure.luau"))()
  
  
  AddButton(Tab1o, {
     Name = "Delete hub ",
    Callback = function()
	  local Settings = {
  JoinTeam = "Pirates"; -- Pirates/Marines
  Translator = true; -- true/false
}

loadstring(game:HttpGet("https://raw.githubusercontent.com/HieuDepTrai-Z/Dev_Delete/refs/heads/main/DeleteHub.lua"))()

AddButton(Tab1o, {
     Name = "Maru hub",
    Callback = function()
	  local Settings = {
  JoinTeam = "Pirates"; -- Pirates/Marines
  Translator = true; -- true/false
}

getgenv().Key = "MARU-4WPUY-7881AO-0VU-VOOV6-FFO63"

getgenv().id = "1367661813751222292"

loadstring(game:HttpGet("https://api.luacrack.site/files/v4/loaders/9fcf46226060786a24d9ca7d89d6bd1b.lua"))()

AddButton(Tab1o, {
     Name = "Vantablack hub English ",
    Callback = function()
	  local Settings = {
  JoinTeam = "Pirates"; -- Pirates/Marines
  Translator = true; -- true/false
}

loadstring(game:HttpGet("https://raw.githubusercontent.com/tranduykhanh08428-web/VantablackxHud/refs/heads/main/VantablackHudBetaEL.lua.txt"))()


AddButton(Tab1o, {
     Name = "Vantablack hub tiếng việt ",
    Callback = function()
	  local Settings = {
  JoinTeam = "Pirates"; -- Pirates/Marines
  Translator = true; -- true/false
}

loadstring(game:HttpGet("https://raw.githubusercontent.com/tranduykhanh08428-web/VantablackxHud/refs/heads/main/VantablackHudBetaVN.lua.txt"))()

AddButton(Tab2o, {
     Name = "Teddy kaitun",
    Callback = function()
	  local Settings = {
  JoinTeam = "Pirates"; -- Pirates/Marines
  Translator = true; -- true/false
}

getgenv()["Config"] = {

    ["Fps Boost"] = true,

    ["FPS Cap"] = 120,

    ["Items"] = {

        ["Auto Fully Fighting Style"] = true,

        ["Skull Guitar"] = true,

        ["Cursed Dual Katana"] = true,

        ["Saber"] = true

    },

    ["Quests"] = {

        ["Mirage Puzzle"] = true,

        ["Upgrading Race"] = true,

    },

    ["Hopping"] = {

        ["Auto Hop"] = true,

        ["Hop Idle"] = true,

        ["High Ping Hop"] = false,

        ["Player Nearing Hop"] = false,

    },

    ["Sniper Fruit Shop"] = {

        ["Enabled"] = true,

        ["Fruit"] = {

            "Leopard-Leopard",

            "Kitsune-Kitsune",

            "Dragon-Dragon",

            "Yeti-Yeti",

            "Gas-Gas"

        },

    },

}

loadstring(game:HttpGet("https://raw.githubusercontent.com/Teddyseetink/diepvyzubu/refs/heads/main/TeddyHub-kaitunBF.lua"))()


AddButton(Tab2o, {
     Name = "Hub ",
    Callback = function()
	  local Settings = {
  JoinTeam = "Pirates"; -- Pirates/Marines
  Translator = true; -- true/false
}

loadstring(game:HttpGet("https://raw.githubusercontent.com/AhmadV3/Speed-Hub-X/main/Speed%20Hub%20X.lua"))()


AddButton(Tab2o, {
     Name = "Maru Hub ",
    Callback = function()
	  local Settings = {
  JoinTeam = "Pirates"; -- Pirates/Marines
  Translator = true; -- true/false
}

loadstring(game:HttpGet("https://raw.githubusercontent.com/xDepressionx/Free/main/MaruHubV2.lua"))()


AddButton(Tab2o, {
     Name = "Zen Hub ",
    Callback = function()
	  local Settings = {
  JoinTeam = "Pirates"; -- Pirates/Marines
  Translator = true; -- true/false
}

loadstring(game:HttpGet("https://raw.githubusercontent.com/ZisHusband/ZisHub/main/ZisRoblox"))()

AddButton(Tab1o, {
     Name = "Redz Hub V2",
    Callback = function()
	  local Settings = {
  JoinTeam = "Pirates"; -- Pirates/Marines
  Translator = true; -- true/false
}

loadstring(game:HttpGet("https://raw.githubusercontent.com/REDZ3112/RedzHubV2/main/Source.lua"))()

AddButton(Tab3o, {
     Name = "fix lag",
    Callback = function()
	  local Settings = {
  JoinTeam = "Pirates"; -- Pirates/Marines
  Translator = true; -- true/false
}

local Players = game:GetService("Players")
local CoreGui = game:GetService("CoreGui")
local Lighting = game:GetService("Lighting")
local Workspace = game:GetService("Workspace")
local RunService = game:GetService("RunService")

-- Tạo Giao Diện (UI)
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "FixLagFPS_Menu"
-- Thử chèn vào CoreGui để không bị mất khi chết, nếu lỗi thì chèn vào PlayerGui
local success, _ = pcall(function() ScreenGui.Parent = CoreGui end)
if not success then ScreenGui.Parent = Players.LocalPlayer:WaitForChild("PlayerGui") end

-- Khung Menu Chính
local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.Size = UUDim2.new(0, 160, 0, 220)
MainFrame.Position = UDim2.new(0, 10, 0, 50) -- Góc trên bên trái
MainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true -- Bạn có thể kéo menu đi chỗ khác nếu vướng

-- Bo góc menu
local UICorner = Instance.new("UICorner", MainFrame)
UICorner.CornerRadius = UDim.new(0, 8)

-- Nhãn Hiển Thị FPS
local FpsLabel = Instance.new("TextLabel", MainFrame)
FpsLabel.Size = UDim2.new(1, 0, 0, 40)
FpsLabel.BackgroundTransparency = 1
FpsLabel.Font = Enum.Font.GothamBold
FpsLabel.TextColor3 = Color3.fromRGB(0, 255, 127)
FpsLabel.TextSize = 18
FpsLabel.Text = "FPS: Loding..."

-- Hàm Tính FPS chuẩn
local lastUpdate = os.clock()
local frameCount = 0
RunService.RenderStepped:Connect(function()
    frameCount = frameCount + 1
    local now = os.clock()
    if now - lastUpdate >= 0.5 then
        local fps = math.round(frameCount / (now - lastUpdate))
        FpsLabel.Text = "⚡ FPS: " .. fps
        frameCount = 0
        lastUpdate = now
    end
end)

-- Hàm xử lý Fix Lag theo cấp độ
local function ÁpDụngFixLag(mucDo)
    pcall(function()
        -- CẤP ĐỘ 25%: Tắt bóng đổ và hiệu ứng ánh sáng nặng
        if mucDo >= 25 then
            Lighting.GlobalShadows = false
            for _, effect in pairs(Lighting:GetChildren()) do
                if effect:IsA("PostEffect") or effect:IsA("BlurEffect") or effect:IsA("BloomEffect") or effect:IsA("SunRaysEffect") then
                    effect.Enabled = false
                end
            end
        end
        
        -- CẤP ĐỘ 50%: 25% + Tắt hiệu ứng hạt (Skill, khói, lửa...)
        if mucDo >= 50 then
            for _, v in pairs(Workspace:GetDescendants()) do
                if v:IsA("ParticleEmitter") or v:IsA("Trail") or v:IsA("Beam") or v:IsA("Smoke") or v:IsA("Fire") then
                    v.Enabled = false
                end
            end
        end
        
        -- CẤP ĐỘ 75%: 50% + Biến mọi chất liệu thành Nhựa trơn (Smooth Plastic) giảm tải GPU
        if mucDo >= 75 then
            local terrain = Workspace:FindFirstChildOfClass("Terrain")
            if terrain then
                terrain.WaterWaveSize = 0
                terrain.WaterWaveSpeed = 0
                terrain.WaterReflectance = 0
                terrain.WaterTransparency = 0
            end
            for _, v in pairs(Workspace:GetDescendants()) do
                if v:IsA("BasePart") and not v:IsA("Terrain") then
                    v.Material = Enum.Material.SmoothPlastic
                end
            end
        end
        
        -- CẤP ĐỘ 100%: POTATO MODE - Xóa sạch hình dán (Decal/Texture), tối giản đồ họa tối đa
        if mucDo == 100 then
            for _, v in pairs(Workspace:GetDescendants()) do
                if v:IsA("Decal") or v:IsA("Texture") then
                    v:Destroy()
                elseif v:IsA("SpecialMesh") then
                    v.TextureId = ""
                end
            end
        end
    end)
end

-- Hàm tạo các nút bấm
local function TaoNut(ten, viTriY, phanTram)
    local Button = Instance.new("TextButton", MainFrame)
    Button.Size = UDim2.new(0, 140, 0, 35)
    Button.Position = UDim2.new(0, 10, 0, viTriY)
    Button.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
    Button.Font = Enum.Font.Gotham
    Button.TextColor3 = Color3.fromRGB(255, 255, 255)
    Button.TextSize = 14
    Button.Text = ten
    Instance.new("UICorner", Button).CornerRadius = UDim.new(0, 5)
    
    Button.MouseButton1Click:Connect(function()
        -- Đổi màu nút để nhận diện đang chọn
        for _, child in pairs(MainFrame:GetChildren()) do
            if child:IsA("TextButton") then child.BackgroundColor3 = Color3.fromRGB(45, 45, 45) end
        end
        Button.BackgroundColor3 = Color3.fromRGB(0, 120, 255)
        
        -- Chạy hàm lag
        ÁpDụngFixLag(phanTram)
    end)
end

-- Khởi tạo 4 nút bấm tương ứng với 4 cấp độ
TaoNut("Fix Lag 25%", 50, 25)
TaoNut("Fix Lag 50%", 90, 50)
TaoNut("Fix Lag 75%", 130, 75)
TaoNut("Fix Lag 100% 💀", 170, 100)


AddButton(Tab1o, {
     Name = "maru ",
    Callback = function()
	  local Settings = {
  JoinTeam = "Pirates"; -- Pirates/Marines
  Translator = true; -- true/false
}

loadstring(game:HttpGet("https://raw.githubusercontent.com/nguyenvan603van-svg/M-i-l-m-script/refs/heads/main/gemini-code-1782717743045.lua.txt


AddButton(Tab1o, {
     Name = "Banana Hud ",
    Callback = function()
	  local Settings = {
  JoinTeam = "Pirates"; -- Pirates/Marines
  Translator = true; -- true/false
}

local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
local Win = Fluent:CreateWindow({Title = "🍌 Banana Hub", SubTitle = "No Key - Anti Lag", TabWidth = 140, Size = UDim2.fromOffset(580, 460), Theme = "Dark"})
local T = {M = Win:AddTab({Title = "Main", Icon = "home"}), C = Win:AddTab({Title = "Combat", Icon = "swords"}), TL = Win:AddTab({Title = "Teleport", Icon = "map-pin"})}

local function SetAura(v)
    local c = game.Players.LocalPlayer.Character
    if not c then return end
    if v and not c:FindFirstChild("BAura") then
        local h = Instance.new("Highlight", c) h.Name = "BAura" h.FillColor, h.OutlineColor, h.FillOpacity = Color3.fromRGB(0,255,0), Color3.fromRGB(0,255,0), 0.2
    elseif not v and c:FindFirstChild("BAura") then c.BAura:Destroy() end
end

T.M:AddDropdown("W", {Title = "Vũ Khí", Values = {"Melee", "Sword", "Fruit"}, Default = "Melee", Callback = function(v) _G.W = v end})
T.M:AddToggle("F", {Title = "Auto Farm Level", Default = false}):OnChanged(function(v) _G.F = v SetAura(v) task.spawn(function() while _G.F do task.wait(0.1) end end) end)
T.M:AddToggle("FA", {Title = "Fast Attack", Default = false}):OnChanged(function(v) _G.FA = v end)
T.M:AddToggle("BM", {Title = "Gom Quái", Default = false}):OnChanged(function(v) _G.BM = v end)
T.C:AddToggle("K", {Title = "Kill Aura", Default = false}):OnChanged(function(v) _G.K = v end)
for i = 1, 3 do T.TL:AddButton({Title = "Dịch chuyển Sea " .. i, Callback = function() print(i) end}) end
Fluent:Notify({Title = "Banana Hub", Content = "Loaded mượt mà, viền xanh lá đã sẵn sàng!", Duration = 3})